openapi: 3.1.0
info:
  title: Emit Appeal Resolved Event API
  description: Emits the immutable CASE_APPEAL_RESOLVED lifecycle audit event.
  version: 1.0.0
servers:
  - url: https://uzvqxpfdxwckhtvvjkzo.supabase.co
paths:
  /functions/v1/read-case-registry:
    get:
      summary: Emit Appeal Resolved Event
      operationId: emitAppealResolvedEvent
      parameters:
        - name: case_id
          in: query
          required: true
          schema:
            type: string
            default: CASE-CT-REAL-001
      responses:
        '200':
          description: Appeal resolved event emitted successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      case_id:
                        type: string
                      case_version:
                        type: integer
                      current_case_status:
                        type: string
